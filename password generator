import random
letters = input("symbols in password")
amount_of_letters = int(input("length of password"))
password = ""

for count in range(amount_of_letters):
    password += random.choice(letters)

print(password)
